pipeline {
    agent any
    stages {
        stage('Build') {
    }
            steps {
                echo "Hello Piyush ! This is Jenkins learning"
                sh "cat index.html"
            }
        }
    }
}

